<%= form_for(@envelope) do |f| %>
  <% if @envelope.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@envelope.errors.count, "error") %> prohibited this envelope from being saved:</h2>

      <ul>
      <% @envelope.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="input-group">
      <div class="input-group-addon">N </div>
    <%= f.text_field :name, :class => "input-lg form-control", "placeholder" => "name" %>
    </div>
  </div>
  <!--<div class="form-group">
    <div class="input-group">
      <div class="input-group-addon">C </div>
    <%= f.text_field :color, :class => "input-lg form-control", "placeholder" => "color"  %>
  </div>
  </div>
  <div class="form-group">
    <div class="input-group">
      <div class="input-group-addon">I </div>
    <%= f.text_field :icon, :class => "input-lg form-control", "placeholder" => "icon"  %>
  </div></div>-->
  <div class="form-group">
    <div class="input-group">
      <div class="input-group-addon">%</div>
    <%= f.number_field :precent, :class => "input-lg form-control", "placeholder" => "income percentage"  %>
  </div>
  </div>
  <div class="form-group">
    <div class="input-group">
      <div class="input-group-addon">$ </div>
        <%= f.number_field :cash, :class => "input-lg form-control", "placeholder" => "cash"  %>
  </div>
  </div>
  <div class="actions" >
    <%= f.submit 'Submit', :class=> "btn btn-warning btn-lg pull-right" %>
  </div>
<% end %>
