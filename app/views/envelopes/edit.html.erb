<h1>Envelope</h1>

<%= render 'form' %>
<p>
<%= link_to 'Delete', @envelope, method: :delete, class: 'btn btn-lg btn-default', data: { confirm: 'Are you sure?' } %>
<!--<%= link_to 'Show', @envelope, class: 'btn btn-lg btn-info' %>-->
<p />
<hr />

<%= render 'newtrans' %>



<br />
<br />
<hr />
<% if @envelope.transactions.count >= 21 %>
  <%= link_to "See Trasaction History", envelope_path(@envelope), class: 'btn btn-lg btn-default', "data-no-turbolink"=>"true" %>
<% else %>
  <%= render 'trans' %>
<% end %>
